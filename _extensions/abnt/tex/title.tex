$if(title)$
\title{
$if(sentence-case)$\MakeTitlecase{$title$}$else$$title$$endif$
$if(thanks)$\thanks{$thanks$}$endif$
}

\titulo{
$if(sentence-case)$\MakeTitlecase{$title$}$else$$title$$endif$
}
$endif$

$if(subtitle)$
\subtitle{$subtitle$}
$endif$

$if(author)$
\author{$for(authors)$$it.name.literal$$sep$ \and $endfor$}
\autor{$for(authors)$$it.name.literal$$sep$ \and $endfor$}
$endif$

$if(location)$
\local{$location$}
$endif$

$if(date)$
\date{$date$}
\data{$date$}
$endif$

$if(supervisor)$
\orientador{$supervisor$}
$endif$

$if(cosupervisor)$
\coorientador{$cosupervisor$}
$endif$

$if(academic-title)$
\tipodetituloacademico{$academic-title$}
$endif$

$if(academic-degree)$
\tituloacademico{$academic-degree$}
$endif$

$if(type-of-work)$
\tipotrabalho{$type-of-work$}
$endif$

$if(area-of-concentration)$
\areadeconcentracao{$area-of-concentration$}
$endif$

$if(university)$
\instituicao{\MakeUppercase{$university$}}
\universidade{$university$}

$if(school)$
\instituicao{
  \MakeUppercase{$university$}
  \par
  \MakeUppercase{$school$}
}

\escola{$school$}
$endif$

$if(program)$
\instituicao{
  \MakeUppercase{$university$}
  \par
  \MakeUppercase{$school$}
  \par
  \MakeUppercase{$program$}
}

\programa{$program$}
$endif$
$endif$

$if(version-note)$
$if(sentence-case)$
\notadeversao{\MakeTitlecase{$version-note$}}
$else$
\notadeversao{$version-note$}
$endif$
$endif$
