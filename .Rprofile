# Load or require packages -----

if (interactive()) {
  suppressMessages(require(cli))
  suppressMessages(require(here))
  suppressMessages(require(magrittr))
  suppressMessages(require(renv))
  suppressMessages(require(stats))
  suppressMessages(require(stringr))
}

# Activate `renv` -----

source(here::here("renv", "activate.R"))

# Set options -----

options(scipen = 999)

# Set system locale -----

source(here::here("R", "set_locale.R"))
